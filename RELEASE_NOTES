A new version of PHPT runner is out. PhptRunner now reports parse errors from a file as a failure (instead of propagating an exception).

For complete list of changes since previous version, see CHANGELOG.md.
